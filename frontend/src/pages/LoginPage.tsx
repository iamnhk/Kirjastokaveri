function LoginPage() {
  return (
    <div className="flex flex-col gap-md">
      <h2 className="text-xl font-semibold text-slate-800">Log in</h2>
      <p className="text-slate-600 text-sm">Authentication forms and validation will be migrated in the dedicated auth milestone.</p>
    </div>
  )
}

export default LoginPage
