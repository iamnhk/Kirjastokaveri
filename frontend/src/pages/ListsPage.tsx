import { PageSection } from '../layouts'

function ListsPage() {
  return (
    <PageSection
      title="Reading lists"
      subtitle="Wishlist and reservations dashboard will land once backend integration is ready."
    />
  )
}

export default ListsPage
