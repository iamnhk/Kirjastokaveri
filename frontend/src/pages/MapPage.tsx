import { PageSection } from '../layouts'

function MapPage() {
  return (
    <PageSection
      title="Library map"
      subtitle="Interactive map and geolocation services will be migrated from the reference project."
    />
  )
}

export default MapPage
